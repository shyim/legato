[package]
name = "legato-rules"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Typed compatibility-rule registry for Legato"
keywords = ["php", "compatibility", "api", "semver"]
categories = ["development-tools"]
readme = "README.md"

[features]
default = []
serde = ["dep:serde"]

[dependencies]
serde = { workspace = true, optional = true }

[dev-dependencies]
serde_json.workspace = true
